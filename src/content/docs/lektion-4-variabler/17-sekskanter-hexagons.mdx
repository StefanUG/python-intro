---
title: "17. Sekskanter (Hexagons)"
sidebar:
  order: 17
---

## Matematik

Computere er rigtig god til matematik.

Der er 360 grader i en cirkel. Så hvis man vil tegne en trekant, skal man lave tre kanter og dreje 120 grader hver gang. 

> Det er fordi 120 gange 3 er 360

For at regne den anden vej, hvis du vil lave tre kanter, skal du dele 360 grader med 3:

    360 ÷ 3 = 120
  

På en computer bruger vi `/` (skråstreg) til at dividere med. Så vi skal skrive

    360 / 3

for at få 120. Resultatet kan man gemme i en variabel, eller sende direkte til en funktion.

```python
# Gem vinklen i en variabel
angle = 360 / 3

# Send vinklen direkte til en funktion
left(360 / 3)
```

Hvis man vil lave en sekskant skal man dividere med 6. Og det kan vi bare få computeren til at regne ud for os.

## Løs Opgaven

Tegn et sejt design med sekskanter

<img src={import.meta.env.BASE_URL + "images/artist-variables/17-sekskanter-hexagons.png"} alt="17. Sekskanter (Hexagons)" class="solution-image" />
    
```python.run
from turtle import *
import random

color_names = ["green", "sky blue", "light blue", "turquoise", "cyan", "slate blue", "medium orchid", "magenta", "aquamarine"]

def random_color(): return random.choice(color_names)

length = ???

for hexagon in range(6):
  color(random_color())
  for side in range(6):
    forward(length)
    left(360 / 6)
  left(60)

done()
```

:::tip[Hint]
En sekskant har 6 sider, og hver drejning er `360 / 6` grader. Opgaven fortæller, at `length` skal være 60.
:::

<Solution>

Length skal være 60 for mønstret i opgaven.

```python.run
from turtle import *
import random

color_names = ["green", "sky blue", "light blue", "turquoise", "cyan", "slate blue", "medium orchid", "magenta", "aquamarine"]

def random_color():
    return random.choice(color_names)

length = 60

for hexagon in range(6):
  color(random_color())
  for side in range(6):
    forward(length)
    left(360 / 6)
  left(60)

done()
```

</Solution>

